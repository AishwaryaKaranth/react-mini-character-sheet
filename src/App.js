import React from "react";
import "./styles.css";

let arr=[1];
let add=(x,y)=>x+y;
let res=arr.reduce(add,2);
console.log(res);

//demo of react reducers

export default function App() {
  return (
    <div className="App">
      
    </div>
  );
}
